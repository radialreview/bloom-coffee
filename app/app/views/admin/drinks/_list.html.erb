<ul id="drinks" class="list-none p-0 m-0 mb-4 flex flex-col gap-2 max-h-[60vh] overflow-y-auto">
  <%= render drinks %>
</ul>

<% if drinks.empty? %>
  <p id="drinks_empty" class="mb-4 text-stone-600">No drinks yet. Add the first one below.</p>
<% end %>
